#include <stdio.h> 


// constants = fixed value that cannot be altered by program during it execution
// preceding = const
//common practice const variables = uppercase
int main() {
    
    const float PI =3.14159;
    printf("PI = %f",PI);
    return 0; 
}